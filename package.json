{
  "name": "jumping",
  "description": "Assign custom aliases to directories and quickly jump to them from anywhere.",
  "version": "1.5.0",
  "author": "Mikey Oz",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/familyfriendlymikey/jumping.git"
  },
  "files": [
    "dist"
  ],
  "bin": {
    "jumping": "bin"
  },
  "scripts": {
    "dev": "imba build -w main.imba",
    "build": "imba build main.imba",
    "prepack": "npm i && npm run build",
    "check": "npm pack --dry-run"
  },
  "dependencies": {
    "colors": "^1.4.0",
    "commander": "^9.4.1"
  }
}
